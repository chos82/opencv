# OpenCV_w_python-binds
# just plaiing around with some cam and detection algorithms of openCV
# It`s an Eclipse- PyDev- Project

goal of project:
- get python to have vectorized contous of detected objects (wich are provided by opencv pretrained models, e.g. face, body, etc.)
- missing (TODO):
	layer several opencv detection algorithms
	apply histograms to detection frames
